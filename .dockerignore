# Python side (harbor-sas root)
__pycache__/
*.pyc
.venv/
.dev-data/
.demo-data/
.anton_backups/
.pytest_cache/
*.egg-info/
.git/
*.db


# Local-only artifacts that must never enter the image
secrets.yaml
*.cast
*.mp4
notes/
assets/
docs/
tests/
umbrel/
vps/
packaging/
install.sh
.DS_Store
.env*
.opencode/
.playwright-mcp/
.swarm/
.github/
progress.md
context.md
fleet/
scripts/

# Anton's own web UI: the image builds it in its own stage from source, so a
# host-side install or build must not enter the context.
anton/web/node_modules/
anton/web/dist/
